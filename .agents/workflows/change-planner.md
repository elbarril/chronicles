---
description: Plan a generic update — feature, refactor, bug fix, infra tweak, or docs-only change — before any code is written.
---

# Change Planner

Execute the canonical skill `.agents/skills/change-planner/SKILL.md`.

## Steps

1. Read `.agents/skills/change-planner/SKILL.md` completely.
2. Execute its steps in order.
3. Hand off to `change-implementer` only after the user confirms the plan.

## Notes

- Skip this workflow for trivial single-file edits already covered by an existing test or convention.
- Do not duplicate logic outside the canonical skill.
