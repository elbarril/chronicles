---
description: Implement a confirmed generic update end-to-end — branch, code, tests, verification, and optional closeout.
---

# Change Implementer

Execute the canonical skill `.agents/skills/change-implementer/SKILL.md`.

## Steps

1. Read `.agents/skills/change-implementer/SKILL.md` completely.
2. Execute its steps in order.
3. Hand off to `change-closeout` only when the change scope warrants documentation or skill updates (see step 6 of the skill).

## Notes

- A confirmed plan from `change-planner` (formal or inline) must exist before this workflow runs.
- Do not duplicate logic outside the canonical skill.
