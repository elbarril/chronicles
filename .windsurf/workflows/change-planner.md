---
description: Plan a generic update — feature, refactor, bug fix, infra tweak, or docs-only change — before any code is written.
---

# Change Planner

Follow exactly the steps defined in `.agents/workflows/change-planner.md`,
which executes the canonical skill `.agents/skills/change-planner/SKILL.md`.

Do not duplicate logic in this file: this stub exists only to enable
`/change-planner` as a slash command in Cascade.
