---
description: Close out a generic update — capture reusable patterns and synchronize project documentation.
---

# Change Closeout

Follow exactly the steps defined in `.agents/workflows/change-closeout.md`,
which executes the canonical skill `.agents/skills/change-closeout/SKILL.md`.

Do not duplicate logic in this file: this stub exists only to enable
`/change-closeout` as a slash command in Cascade.
