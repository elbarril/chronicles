---
trigger: always_on
description: Integración con AGENTS.md del repo Chronicle
---

# Bootstrap de Agente

Este repositorio sigue la convención AGENTS.md.

Antes de cualquier acción:

1. Leer `AGENTS.md` en la raíz del repo.
2. Leer `.agents/memory/project-context.md` y `.agents/memory/decisions.md`.
3. Si la tarea afecta `.agents/`, invocar el skill `agent-workspace-manager` en `.agents/skills/agent-workspace-manager/SKILL.md`.

Idioma de respuesta: español rioplatense, salvo pedido explícito.
